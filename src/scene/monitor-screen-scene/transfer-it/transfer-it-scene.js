export default class TransferItScene extends THREE.Group {
  constructor(data) {
    super();

    this._scene = data.scene;
    this._camera = data.camera;

    this._init();
  }

  update(dt) {

  }

  _init() {

  }
}
